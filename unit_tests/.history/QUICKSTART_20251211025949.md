# ⚡ Quick Start Guide - CI/CD для Quizizz Tests

## 🎯 Швидкий старт (5 хвилин)

### Крок 1: Встановіть залежності

```powershell
# Windows PowerShell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

```bash
# Linux/macOS
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Крок 2: Запустіть тести

```powershell
# Запустити всі тести
pytest tests/ -v

# З паралелізацією (швидше)
pytest tests/ -n auto -v

# З Allure звітами
pytest tests/ --alluredir=allure-results -v
allure serve allure-results
```

### Крок 3: Запустіть локальний CI/CD pipeline

```powershell
# Windows
.\run_ci_locally.ps1

# Linux/macOS
chmod +x run_ci_locally.sh
./run_ci_locally.sh
```

---

## 🏗️ Jenkins Setup (10 хвилин)

### 1. Встановіть Jenkins

**Windows:**
```powershell
choco install jenkins
```

**Linux:**
```bash
docker run -p 8080:8080 jenkins/jenkins:lts
```

### 2. Відкрийте Jenkins
- Браузер: `http://localhost:8080`
- Отримайте пароль: `cat /var/lib/jenkins/secrets/initialAdminPassword`

### 3. Встановіть плагіни
- Git
- Pipeline
- Allure
- HTML Publisher

### 4. Створіть Pipeline Job
- New Item → Pipeline
- SCM: Git
- Script Path: `Jenkinsfile`
- Build Triggers: Poll SCM `H/5 * * * *`

### 5. Запустіть білд
- Build Now

---

## 🐙 GitHub Actions (3 хвилини)

### 1. Push до GitHub

```bash
git init
git add .
git commit -m "Add CI/CD configuration"
git remote add origin <your-repo>
git push -u origin main
```

### 2. Workflow запуститься автоматично
- Перейдіть до **Actions** tab у GitHub
- Спостерігайте за виконанням pipeline

### 3. Перегляньте Allure звіт
- Після завершення workflow
- Завантажте artifacts `allure-results-*`

---

## 📊 BDD Tests

### Запуск BDD тестів

```powershell
# Всі BDD тести
pytest tests/bdd/ -v

# З Allure
pytest tests/bdd/ --alluredir=allure-results -v
```

### Feature файли

- `tests/bdd/features/quiz_search.feature` - Пошук квізів
- `tests/bdd/features/quiz_library.feature` - Бібліотека квізів

---

## 🎨 Allure Reports

### Генерація звіту

```powershell
# 1. Встановіть Allure
scoop install allure  # Windows
# або
brew install allure   # macOS

# 2. Запустіть тести
pytest tests/ --alluredir=allure-results

# 3. Відкрийте звіт
allure serve allure-results
```

---

## 🔥 Корисні команди

```powershell
# Запустити smoke тести
pytest -m smoke -v

# Запустити без повільних тестів
pytest -m "not slow" -v

# Покриття коду
pytest tests/ --cov=. --cov-report=html
# Звіт: reports/coverage/index.html

# Один файл
pytest tests/test_quiz_search.py -v

# Один тест
pytest tests/test_quiz_search.py::TestQuizSearch::test_basic_search -v
```

---

## 📁 Важливі файли

| Файл | Призначення |
|------|-------------|
| `Jenkinsfile` | Jenkins Pipeline конфігурація |
| `.github/workflows/ci-cd.yml` | GitHub Actions workflow |
| `pytest.ini` | Конфігурація pytest |
| `requirements.txt` | Python залежності |
| `CI_CD_SETUP.md` | Детальна інструкція |

---

## 🆘 Troubleshooting

### Проблема: ModuleNotFoundError

**Рішення:**
```powershell
pip install -r requirements.txt
```

### Проблема: Jenkins не запускається

**Рішення:**
```powershell
# Перевірте статус
Get-Service jenkins

# Перезапустіть
Restart-Service jenkins
```

### Проблема: Allure не генерується

**Рішення:**
```powershell
# Перевстановіть
pip uninstall allure-pytest
pip install allure-pytest

# Перевірте версію
allure --version
```

---

## 📚 Наступні кроки

1. ✅ Запустіть тести локально
2. ✅ Налаштуйте Jenkins або GitHub Actions
3. ✅ Додайте власні BDD тести
4. ✅ Налаштуйте Git webhooks
5. ✅ Інтегруйте з ReportPortal (опційно)

---

## 🎓 Детальна документація

Повна інструкція: [`CI_CD_SETUP.md`](CI_CD_SETUP.md)

---

**Готово! Ваш CI/CD pipeline налаштований! 🎉**
