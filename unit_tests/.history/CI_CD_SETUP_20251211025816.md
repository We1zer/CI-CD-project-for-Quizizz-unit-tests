# 🚀 CI/CD Setup для Quizizz Test Framework

## 📋 Зміст
1. [Огляд проєкту](#огляд-проєкту)
2. [Налаштування Jenkins](#налаштування-jenkins)
3. [Налаштування GitHub Actions](#налаштування-github-actions)
4. [Запуск тестів локально](#запуск-тестів-локально)
5. [Allure Reports](#allure-reports)
6. [Структура проєкту](#структура-проєкту)

---

## 🎯 Огляд проєкту

Цей проєкт містить **159+ unit-тестів** для тестування Quizizz Test Framework з повною інтеграцією CI/CD через Jenkins та GitHub Actions.

### Особливості:
- ✅ Паралельне виконання тестів (pytest-xdist)
- ✅ BDD тести з pytest-bdd
- ✅ Allure звітність
- ✅ Coverage аналіз
- ✅ Автоматичні білди при кожному коміті
- ✅ Підтримка Jenkins та GitHub Actions

---

## 🛠️ Налаштування Jenkins

### Крок 1: Встановлення Jenkins

#### Windows:
```powershell
# Завантажте Jenkins з офіційного сайту
# https://www.jenkins.io/download/

# Або через Chocolatey
choco install jenkins
```

#### Linux/macOS:
```bash
# Через Docker
docker run -p 8080:8080 -p 50000:50000 jenkins/jenkins:lts

# Або встановлення пакету
# Ubuntu
wget -q -O - https://pkg.jenkins.io/debian/jenkins.io.key | sudo apt-key add -
sudo sh -c 'echo deb https://pkg.jenkins.io/debian-stable binary/ > /etc/apt/sources.list.d/jenkins.list'
sudo apt update
sudo apt install jenkins
```

### Крок 2: Перший запуск Jenkins

1. Відкрийте браузер: `http://localhost:8080`
2. Введіть initial admin password:
   ```powershell
   # Windows
   Get-Content "C:\Program Files\Jenkins\secrets\initialAdminPassword"
   
   # Linux
   cat /var/lib/jenkins/secrets/initialAdminPassword
   ```
3. Виберіть "Install suggested plugins"
4. Створіть admin користувача

### Крок 3: Встановлення необхідних плагінів

Перейдіть до: **Manage Jenkins → Manage Plugins → Available**

Встановіть:
- ✅ Git plugin
- ✅ Pipeline plugin
- ✅ Allure plugin
- ✅ HTML Publisher plugin
- ✅ JUnit plugin
- ✅ Workspace Cleanup plugin
- ✅ Build Timeout plugin

### Крок 4: Налаштування Python в Jenkins

1. **Manage Jenkins → Global Tool Configuration**
2. Додайте Python:
   - Name: `Python 3.11`
   - Install automatically або вкажіть шлях до Python

### Крок 5: Встановлення Allure

#### Windows:
```powershell
# Через Scoop
scoop install allure

# Вручну
# 1. Завантажте з https://github.com/allure-framework/allure2/releases
# 2. Розпакуйте в C:\allure
# 3. Додайте C:\allure\bin до PATH
```

#### Linux:
```bash
# Через package manager
sudo apt-add-repository ppa:qameta/allure
sudo apt-get update
sudo apt-get install allure
```

### Крок 6: Налаштування Allure в Jenkins

1. **Manage Jenkins → Global Tool Configuration**
2. Знайдіть **Allure Commandline**
3. Додайте інсталяцію:
   - Name: `Allure`
   - Version: `2.24.0` або встановіть автоматично

### Крок 7: Створення Pipeline Job

1. **New Item → Pipeline**
2. Назвіть: `Quizizz-Tests-CI`
3. В **Pipeline** секції:
   - Definition: `Pipeline script from SCM`
   - SCM: `Git`
   - Repository URL: `<your-git-repo>`
   - Script Path: `Jenkinsfile`

4. **Build Triggers**:
   - ✅ Poll SCM: `H/5 * * * *` (кожні 5 хвилин)
   - ✅ GitHub hook trigger (якщо використовуєте GitHub)

### Крок 8: Налаштування Git Webhooks (опційно)

Для автоматичного запуску після кожного push:

1. В GitHub: **Settings → Webhooks → Add webhook**
2. Payload URL: `http://<jenkins-url>:8080/github-webhook/`
3. Content type: `application/json`
4. Events: `Just the push event`

---

## 🔄 Налаштування GitHub Actions

### Крок 1: Створення репозиторію

```bash
cd unit_tests
git init
git add .
git commit -m "Initial commit with CI/CD setup"
git remote add origin <your-github-repo>
git push -u origin main
```

### Крок 2: Активація GitHub Actions

Workflow файл вже створено в `.github/workflows/ci-cd.yml`. GitHub Actions запуститься автоматично після push.

### Крок 3: Налаштування Secrets (якщо потрібно)

В GitHub репозиторії:
1. **Settings → Secrets and variables → Actions**
2. Додайте необхідні secrets (наприклад, API keys)

### Крок 4: Перегляд Allure Reports

Після запуску workflow:
1. Перейдіть до **Actions** tab
2. Виберіть останній workflow run
3. Завантажте artifacts з назвою `allure-results-*`

Для публікації на GitHub Pages:
1. **Settings → Pages**
2. Source: `gh-pages` branch
3. Allure звіт буде доступний на `https://<username>.github.io/<repo>/`

---

## 💻 Запуск тестів локально

### Встановлення залежностей

```powershell
# Створення віртуального середовища
python -m venv venv

# Активація (Windows PowerShell)
.\venv\Scripts\Activate.ps1

# Активація (Linux/macOS)
source venv/bin/activate

# Встановлення залежностей
pip install -r requirements.txt
```

### Запуск всіх тестів

```powershell
# Основні тести
pytest tests/ -v

# З паралелізацією
pytest tests/ -n auto -v

# З Allure звітами
pytest tests/ --alluredir=allure-results -v

# З coverage
pytest tests/ --cov=. --cov-report=html -v
```

### Запуск BDD тестів

```powershell
# Всі BDD тести
pytest tests/bdd/ -v

# Конкретний feature
pytest tests/bdd/test_quiz_search_bdd.py -v

# З маркерами
pytest -m bdd -v
```

### Запуск конкретних тестів

```powershell
# Один тестовий файл
pytest tests/test_quiz_search.py -v

# Один тестовий клас
pytest tests/test_quiz_search.py::TestQuizSearch -v

# Один тестовий метод
pytest tests/test_quiz_search.py::TestQuizSearch::test_basic_search -v

# За маркерами
pytest -m "smoke" -v
pytest -m "not slow" -v
```

### Генерація Allure звіту

```powershell
# 1. Запустіть тести з Allure
pytest tests/ --alluredir=allure-results

# 2. Згенеруйте звіт
allure generate allure-results -o allure-report --clean

# 3. Відкрийте звіт
allure open allure-report
```

---

## 📊 Allure Reports

### Можливості Allure

Allure надає детальні звіти з:
- ✅ Графіки успішності тестів
- ✅ Історія виконання
- ✅ Категорії помилок
- ✅ Timeline виконання
- ✅ Attachments (screenshots, logs)
- ✅ BDD steps

### Структура звіту

```
allure-report/
├── index.html          # Головна сторінка
├── widgets/            # Графіки та віджети
├── data/              # Дані тестів
└── export/            # Експорт даних
```

### Інтеграція в тестах

```python
import allure

@allure.feature('Quiz Search')
@allure.story('Basic Search')
@allure.severity(allure.severity_level.CRITICAL)
def test_search():
    with allure.step('Open search page'):
        # код
    
    with allure.step('Enter search term'):
        # код
    
    allure.attach('Search results', name='Results', 
                  attachment_type=allure.attachment_type.TEXT)
```

---

## 📁 Структура проєкту

```
unit_tests/
├── .github/
│   └── workflows/
│       └── ci-cd.yml           # GitHub Actions workflow
│
├── tests/
│   ├── bdd/                    # BDD тести
│   │   ├── features/           # Gherkin feature файли
│   │   │   ├── quiz_search.feature
│   │   │   └── quiz_library.feature
│   │   ├── test_quiz_search_bdd.py
│   │   └── test_quiz_library_bdd.py
│   │
│   ├── test_quiz_search.py     # Unit тести пошуку
│   ├── test_quiz_library.py    # Unit тести бібліотеки
│   ├── test_subject_categories.py
│   └── ...                     # Інші тести
│
├── reports/                    # Звіти тестування
│   ├── report.html
│   ├── junit.xml
│   └── coverage/
│
├── allure-results/             # Allure дані
├── allure-report/              # Згенерований Allure звіт
│
├── Jenkinsfile                 # Jenkins Pipeline
├── pytest.ini                  # Pytest конфігурація
├── requirements.txt            # Python залежності
└── CI_CD_SETUP.md             # Ця інструкція
```

---

## 🔧 Конфігурація pytest

Файл `pytest.ini`:

```ini
[pytest]
minversion = 7.0
testpaths = tests
addopts = -v --strict-markers --tb=short

markers =
    smoke: швидкі smoke тести
    integration: інтеграційні тести
    bdd: BDD тести
    slow: повільні тести
```

---

## 🐛 Налагодження

### Jenkins не запускає білд

1. Перевірте Poll SCM налаштування
2. Перевірте Git credentials
3. Перегляньте Jenkins logs: `http://localhost:8080/log/all`

### Allure не генерується

```powershell
# Перевірте версію Allure
allure --version

# Перевстановіть плагін
pip uninstall allure-pytest
pip install allure-pytest
```

### Тести падають в CI

1. Перевірте Python версію в CI
2. Переконайтеся, що всі залежності встановлені
3. Перевірте логи GitHub Actions

---

## 📈 Покриття коду

Переглянути покриття:

```powershell
# Генерація звіту
pytest tests/ --cov=. --cov-report=html

# Відкрийте reports/coverage/index.html
```

---

## 🚦 CI/CD Workflow

### Jenkins Pipeline

```
Checkout → Install Deps → Lint → Test (Parallel) → BDD Tests → Coverage → Allure Report
```

### GitHub Actions

```
Matrix [OS x Python] → Test → Coverage → Upload Artifacts → Generate Allure → Deploy to Pages
```

---

## 📞 Корисні посилання

- [Jenkins Documentation](https://www.jenkins.io/doc/)
- [Allure Framework](https://docs.qameta.io/allure/)
- [pytest Documentation](https://docs.pytest.org/)
- [GitHub Actions](https://docs.github.com/en/actions)

---

## ✅ Чеклист завершення

- [ ] Jenkins встановлено та налаштовано
- [ ] Всі плагіни встановлені
- [ ] Pipeline створено та працює
- [ ] Git webhooks налаштовані
- [ ] Allure звіти генеруються
- [ ] GitHub Actions активовано
- [ ] Тести проходять локально
- [ ] Coverage > 80%
- [ ] BDD тести додані
- [ ] Документація актуальна

---

**Створено**: 11 Грудня 2024
**Версія**: 1.0.0
**Автор**: Quizizz Test Framework Team
